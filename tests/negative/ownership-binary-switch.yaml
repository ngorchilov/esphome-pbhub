esphome:
  name: pbhub-own-binary-switch

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

binary_sensor:
  - platform: pbhub
    id: input_conflict
    pbhub_id: test_hub
    channel: 3
    signal: a

switch:
  - platform: pbhub
    id: switch_conflict
    pbhub_id: test_hub
    channel: 3
    signal: a
