esphome:
  name: pbhub-unsupported-rtttl-output

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: fixed_pwm
    pbhub_id: test_hub
    channel: 0
    signal: a
    mode: pwm

rtttl:
  output: fixed_pwm
