esphome:
  name: pbhub-legacy-pin-field

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: legacy_output
    pbhub_id: test_hub
    pin: 0
    mode: pwm
