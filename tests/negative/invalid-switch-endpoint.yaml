esphome:
  name: pbhub-invalid-switch-pin

packages:
  common: !include ../common/esp32-arduino.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

switch:
  - platform: pbhub
    id: invalid_switch
    pbhub_id: test_hub
    pin: 9
