esphome:
  name: pbhub-own-adc-binary

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

sensor:
  - platform: pbhub
    id: adc_conflict
    pbhub_id: test_hub
    channel: 2

binary_sensor:
  - platform: pbhub
    id: input_conflict
    pbhub_id: test_hub
    channel: 2
    signal: a
