esphome:
  name: pbhub-servo-frequency

packages:
  common: !include ../common/esp32-arduino.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: invalid_frequency
    pbhub_id: test_hub
    pin: 0
    mode: servo
    frequency: 60Hz
