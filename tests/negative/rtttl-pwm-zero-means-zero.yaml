esphome:
  name: pbhub-rtttl-pwm-zero-means-zero

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: unsafe_zero_pwm
    pbhub_id: test_hub
    channel: 0
    signal: b
    mode: pwm

rtttl:
  output: unsafe_zero_pwm
