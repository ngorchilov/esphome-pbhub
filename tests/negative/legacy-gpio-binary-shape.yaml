esphome:
  name: pbhub-legacy-gpio-binary

packages:
  common: !include ../common/esp32-arduino.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

binary_sensor:
  - platform: gpio
    id: legacy_input
    pin:
      pbhub: test_hub
      number: 0
