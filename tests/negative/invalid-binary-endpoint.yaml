esphome:
  name: pbhub-invalid-binary-pin

packages:
  common: !include ../common/esp32-arduino.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

binary_sensor:
  - platform: pbhub
    id: invalid_input
    pbhub_id: test_hub
    pin: 2
