esphome:
  name: pbhub-servo-zero-false

packages:
  common: !include ../common/esp32-arduino.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: invalid_servo_output
    pbhub_id: test_hub
    pin: 0
    mode: servo
    zero_means_zero: false
