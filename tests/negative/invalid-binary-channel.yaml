esphome:
  name: pbhub-invalid-binary-channel

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

binary_sensor:
  - platform: pbhub
    id: invalid_input
    pbhub_id: test_hub
    channel: 6
    signal: a
