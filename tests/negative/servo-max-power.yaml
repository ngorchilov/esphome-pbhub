esphome:
  name: pbhub-servo-max-power

packages:
  common: !include ../common/esp32-idf.yaml

pbhub:
  id: test_hub
  i2c_id: test_i2c

output:
  - platform: pbhub
    id: invalid_servo_output
    pbhub_id: test_hub
    pin: 0
    mode: servo
    max_power: 99%
